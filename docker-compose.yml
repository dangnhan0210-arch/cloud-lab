services:
  backend:
    build: ./server
    ports:
      - "5000:5000"
    enviroment:
      PORT: 5000
      MONGODB_URI: ${MONGODB_URI}


  frontend:
    build: ./client
    pront:
      - "3000:3000"


