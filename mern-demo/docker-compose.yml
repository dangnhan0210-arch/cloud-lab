services:

  backend:
    build: ./server
    container_name: mern-backend
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env

  frontend:
    build: ./client
    container_name: mern-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend